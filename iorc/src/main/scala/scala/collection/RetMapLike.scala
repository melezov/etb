/*
package scala.collection
import generic._

trait RetMapLike[A, +This <: RetMap[A] with RetMapLike[A, This]] extends Retaining[A] with MapLike[A, This] {
self =>
  override def stringPrefix = "RetMap"
  override def keyMap = repr

  override def youngestKey: A = head
  override def oldestKey: A = last

}
*/
